name: flask_app_dev_env
channels:
  - conda-forge
  - defaults
dependencies:
  # Core dependencies
  - python=3.9
  - flask
  - flask-cors
  - boto3
  - requests
  - pip
  # Development dependencies
  - pytest
  - jupyter
  # Pip dependencies
  - pip:
      - python-dotenv
